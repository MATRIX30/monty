#inlclude "monty.h"

/**
 * add - adds the top two elements of the stack
 * 
 * Description: - Usage: add
 * If the stack contains less than two elements, print 
 * the error message L<line_number>: can't add, stack too
 *  short, followed by a new line, and exit with the status EXIT_FAILURE
 * The result is stored in the second top element of the stack, and the top element is removed,
 *  so that at the end:
*/
void add(stack_t **stack_h, unsigned int n)
{

}

void sub
div
mul
mod
